using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VectorField_Exporter : MonoBehaviour
{
    void CreateVf()
    {
        //Create the FourCC
        byte[,,,] FOURCC = new byte['V', 'F', '_', 'V'];

    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
